# ============================================
# Stokvel Backend Configuration
# ============================================
# SETUP: Copy this file to .env
#   Windows: copy .env.example .env
#   Mac/Linux: cp .env.example .env
# Then fill in your actual values below.
# ============================================

# Server
PORT=5000
NODE_ENV=development

# MySQL Database
# Make sure MySQL is running before starting the server
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=stokvel_db

# JWT (change this to a random string in production)
JWT_SECRET=stokvel-dev-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Email (SMTP) - for password reset (optional for development)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=aubreymlangeni01@gmail.com
SMTP_PASS=kjrameqwfkiqrxim

# Frontend URL (CORS)
FRONTEND_URL=http://localhost:5173

# Paystack Payment Gateway (optional for development)
# Get your keys from https://dashboard.paystack.com/#/settings/developer
PAYSTACK_SECRET_KEY=sk_test_ebf7f5cc640eed0a4581039aafc3870839a57aa3
